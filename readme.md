If you haven't installed dependencies yet, first run:

```
npm install
```

If you need to set up the database, run:
```
npm run db:push
```

Development Mode:
```
npm run dev
```

Production Mode:
```
npm run build
npm start
```